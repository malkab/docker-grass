#!/bin/bash

# rsyncs to a remote machine to build there

USERNAME=XXX
HOST=XXX
SOURCEFOLDER=./dangerous_deer
REMOTEFOLDER=$USERNAME@$HOST:~
SSHPORT=22

rsync -avzhr --delete --progress --rsh="ssh -p ${SSHPORT}" $SOURCEFOLDER $REMOTEFOLDER
