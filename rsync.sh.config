#!/bin/bash

# rsyncs to a remote machine to build there

USERNAME=XXX
HOST=XXX
SOURCEFOLDER=XXX
REMOTEFOLDER=XXX
SSHPORT=22

# ---

rsync -avzhr --delete --progress --rsh="ssh -p ${SSHPORT}" $SOURCEFOLDER $USERNAME@$HOST:$REMOTEFOLDER
